pipeline {
    agent {
        label 'docker'
    }
    // environment {


    // }
    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/lokeshkamalay/simple-java-maven-app'
            }
        }
        stage('Build') {
            agent {
                docker {
                    image 'maven'
                    label 'docker'
                }
            }
            steps {
                sh "mvn clean package"
            }
        }
        stage('verify the output') {
            steps {
                sh "ls -latr"
            }
        }
    }
}
